# Story template

## Story ID and title
S237 - Orthogonalized continuous decision memo

## User value
As a research lead, I want the bounded orthogonalized branch closed cleanly after its first packet, so the repo does not drift into low-value iteration on a branch that improved fairness but did not produce a witness win.

## Acceptance criteria
- Record the packet outcome
- Record the stop decision
- Return the branch to memo-only posture

## Outputs
- `docs/research/`
- `docs/evidence/`
- `logs/checkpoint.json`

## Dependencies
- S236

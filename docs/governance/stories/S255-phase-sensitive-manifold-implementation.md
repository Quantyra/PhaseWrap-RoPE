# Story template

## Story ID and title
S255 - Phase-sensitive manifold implementation

## User value
As a research lead, I want the approved phase-sensitive manifold branch implemented and run on its fixed packet, so the repo can test whether state-conditioned phase structure survives the bounded nonlinear control stack.

## Acceptance criteria
- Implement only the approved task, candidate, and controls
- Run the fixed packet
- Emit auditable centering, feature-audit, and regression diagnostics

## Outputs
- `src/qrope/`
- `tests/`
- `logs/ablation_runs/`
- `docs/research/`
- `docs/evidence/`
- `logs/checkpoint.json`

## Dependencies
- S254

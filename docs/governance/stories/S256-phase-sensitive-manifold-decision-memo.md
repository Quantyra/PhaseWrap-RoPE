# Story template

## Story ID and title
S256 - Phase-sensitive manifold decision memo

## User value
As a research lead, I want the bounded phase-sensitive branch closed cleanly after its first packet, so the repo does not drift into low-value iteration on a failed task.

## Acceptance criteria
- Record the packet outcome
- Record the stop decision
- Return the branch to memo-only posture

## Outputs
- `docs/research/`
- `docs/evidence/`
- `logs/checkpoint.json`

## Dependencies
- S255

# Story template

## Story ID and title
S263 - Latent phase manifold implementation

## User value
As a research lead, I want the approved latent phase manifold branch implemented and run on its fixed packet, so the repo can test whether the new latent residual survives the fixed control stack.

## Acceptance criteria
- Implement only the approved task, candidate, and controls
- Run the fixed packet
- Emit auditable generator and regression diagnostics

## Outputs
- `src/qrope/`
- `tests/`
- `logs/ablation_runs/`
- `docs/research/`
- `docs/evidence/`
- `logs/checkpoint.json`

## Dependencies
- S262

# Story template

## Story ID and title
S319 - Transition orbit task design memo

## User value
As a research lead, I want one materially new memo-only angle preserved, so the line can restart later without reusing the failed token-invariant task.

## Acceptance criteria
- define the next task family at memo level
- explain why it differs from the failed branch
- keep implementation closed

## Outputs
- `docs/research/`
- `docs/evidence/`
- `logs/checkpoint.json`

## Dependencies
- S318

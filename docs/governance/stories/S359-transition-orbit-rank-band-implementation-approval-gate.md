# Story template

## Story ID and title
S359 - Transition orbit rank-band implementation approval gate

## User value
As a research lead, I want a strict implementation gate for the rank-band line, so code only reopens if the symbolic ordinal-lookup shortcut is blocked by construction.

## Acceptance criteria
- define exact approval scope
- require rank-band diagnostics explicitly
- keep the phase local-only and zero-credit

## Outputs
- `docs/research/`
- `docs/evidence/`
- `logs/checkpoint.json`

## Dependencies
- S358

# Q-RoPE Bridge Anchor-Distance Post-Packet Decision v1

## Decision
- `CONTINUE UNDER RETAINED-MODEL HARDENING ONLY`

## Reason
- The witness led on mean `mae` and mean `rank_correlation`, so the line did not fail the declared stop rule.
- The result is still weak enough that only the standard retained-model hardening cycle is justified.

## Next Fixed Packet
- `token_permutation = cdab`

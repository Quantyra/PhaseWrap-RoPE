# Q-RoPE Bridge Anchor-Distance Preservation Decision v1

## Decision
- Preserve `anchor-distance` as a weak surviving bounded bridge-task result.

## Reason
- The line survived the full retained-model hardening cycle under the declared stop rule.
- It is materially more relevant to positional behavior than ordinary transfer families.
- It is weaker than `anchor-order`, so the package should preserve it with that limitation stated explicitly.

## Operating Consequence
- Keep `anchor-distance` in the internal package as secondary bridge evidence.
- Do not treat it as a strong bridge result.
- Open another bridge-task line only if it is materially different from both `anchor-order` and `anchor-distance`.

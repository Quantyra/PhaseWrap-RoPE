# Q-RoPE Bridge Anchor-Order Preservation Decision v1

## Decision
- Preserve `anchor-order` as the first positive bounded bridge-task relevance result.

## Reason
- The line survived the full bounded hardening cycle.
- It is closer to anchor-relative positional behavior than the earlier transfer ladder.
- It improves the internal package by adding first-pass relevance-bridge evidence rather than just more transfer-count evidence.

## Operating Consequence
- Keep `anchor-order` in the internal package as bridge-task evidence.
- Do not reopen default perturbation expansion on this family.
- Open another bridge-task line only if it is materially different from `anchor-order` and clears the existing bridge admissibility bar.

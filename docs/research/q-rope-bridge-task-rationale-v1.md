# Q-RoPE Bridge-Task Rationale v1

## Decision
- Open a memo-only bridge-task track.

## Reason
- The current survivor/failure transfer portfolio is already strong enough for low-intensity internal continuation.
- Additional transfer-family count is no longer the main missing evidence.
- The missing evidence is a bridge from the internal relational-mechanism signal toward tasks that are closer to real positional encoding behavior.

## Goal
- Define one task family that remains auditable and bounded, but is less abstract than the current transfer ladder and more directly relevant to positional-encoding behavior.

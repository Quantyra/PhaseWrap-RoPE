# Chart-transition post-cross-direction decision

## Decision
- keep the chart-transition manifold branch active
- do not broaden the task yet
- set one bounded next fairness test

## Why
The witness remained stronger than the cross-direction interaction control on both the primary metric and rank correlation. The gap is now small but still consistent, so the next bounded fairness question is whether one fixed quadratic transition-family expansion is enough.

## Next bounded question
- can a bounded quadratic transition-family control match the witness?

## Why this is the right next control
The branch has now survived linear directional controls and one bounded family of forward-reversed interaction terms. The next smallest remaining fairness question is whether a fixed quadratic basis over the declared transition family closes the gap without introducing chart-id shortcuts or uncontrolled feature growth.

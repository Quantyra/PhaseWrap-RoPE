# Chart-transition manifold post-packet decision

## Decision
- keep the chart-transition manifold branch active
- do not broaden the task yet
- set one bounded next pressure test

## Why
The candidate beat every fixed control on the primary regression metric and kept strong positive rank correlation across the fixed three-seed packet. That is the strongest first-packet witness result in the current manifold line.

## Next bounded question
- can an unordered-transition symbolic control match the witness?

## Why this is the right next control
The current transition-additive control was the strongest approved baseline and came closest on mean MAE. The smallest remaining fairness question is whether the witness win depends on ordered transition structure or whether unordered transition structure is already enough.

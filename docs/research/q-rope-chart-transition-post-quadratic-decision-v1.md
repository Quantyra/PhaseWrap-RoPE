# Chart-transition post-quadratic decision

## Decision
- keep the chart-transition manifold branch active
- do not broaden the task yet
- set one bounded next fairness test

## Why
The witness remained stronger than the quadratic control on both the primary metric and rank correlation. Since the quadratic basis did not close the gap, the remaining symbolic fairness question is whether one fixed cubic transition-family basis is enough.

## Next bounded question
- can a bounded cubic transition-family control match the witness?

## Why this is the right next control
The branch has now survived linear, directional, and quadratic symbolic controls. The next smallest remaining fairness question is whether one explicit cubic basis over the same declared transition-family features can match the witness without opening the door to uncontrolled model families.

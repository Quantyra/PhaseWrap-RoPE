# Chart-transition post-unordered decision

## Decision
- keep the chart-transition manifold branch active
- do not broaden the task yet
- set one bounded next fairness test

## Why
The witness remained stronger than the unordered-transition control on the primary metric and on rank correlation. That means the current task still has uniqueness value beyond unordered transition structure.

## Next bounded question
- can a transition-family permutation control match the witness?

## Why this is the right next control
The branch has now survived the strongest approved fixed control stack and the unordered transition collapse. The next smallest remaining fairness question is whether the witness win depends on one favorable hand-coded ordered transition family rather than ordered transition structure itself.

# Q-RoPE Epic Portfolio v2

## BLUF
- The Q-RoPE research program should remain organized as one program with multiple stretch-effort epics.
- `E001` remains the preserved foundation epic.
- `E002` is now preserved as bounded positional selection robustness evidence.
- `E003` is opened as the next memo-only stretch effort: bounded position-content compositionality.

## Program Structure
- program:
  - Q-RoPE research program
- epics:
  - `E001` foundation and internal evidence package
  - `E002` bounded positional selection robustness
  - `E003` bounded position-content compositionality

## Epic Roles
### E001
- preserve benchmark, transfer, bridge, realism-bridge, and initial successor-class evidence
- maintain the standing internal package

### E002
- test whether successor-class signal survives bounded candidate-set variability
- preserve or stop based on bounded robustness evidence

### E003
- test whether the signal survives bounded composition of positional structure with content-key relevance
- stop immediately if the task collapses into token lookup or fairness blow-up

## Why This Structure
- The repo now has enough preserved evidence that the next stretch effort must be materially different, not another nearby positional variant.
- Opening `E003` keeps the next big question visible at the epic level while leaving execution closed until gates are passed.
- It also makes the gap explicit: current evidence is strong on bounded position-only structure, not yet on bounded position-content composition.

# Q-RoPE Executive Summary v22

Date: 2026-03-15

## BLUF
- Q-RoPE now has an archived negative boundary on bounded shared-memory multi-query selection from `E005`.
- The preserved evidence package is unchanged from the `E004` state, but the boundary map is stronger.
- The program still supports only an internal low-intensity continuation decision.
- Hardware reopening and external claims remain unsupported.

## Strongest Current Claim
- selective positional relevance with one surviving realism-bridge retrieval task, two surviving bounded successor-class selection tasks, one surviving bounded variable-cardinality robustness task, one surviving bounded position-content compositionality task, and one surviving bounded content-alias disambiguation task.

## Boundary Update
- `shared-memory-multi-query-selection` survived the fixed first packet but failed immediately at retained nuisance hardening `token_permutation=cdab`.
- It therefore belongs in the archived negative set, not the preserved survivor set.

## Decision
- continue at low intensity with no new execution.

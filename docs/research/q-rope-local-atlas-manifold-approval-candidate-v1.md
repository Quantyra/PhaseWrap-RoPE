# Local atlas manifold approval-candidate memo

## Decision
- elevate the local atlas manifold path to approval-candidate posture

## Why this path qualifies
The path now has:
- one exact task
- one exact future candidate
- one explicit fixed control stack
- one explicit first packet
- one explicit rejection rule against the currently exposed shortcut families

## Why it is still not approved in this step
The path remains memo-only here. No implementation is justified until a future approval gate confirms that the chart-assignment rule and the bounded single-chart control are sufficiently explicit and fair.

## Next valid move
- write the strict implementation-approval gate for `synthetic_dual_local_atlas_manifold_response`
- keep the branch local-only, zero-credit, and single-candidate if approval is granted

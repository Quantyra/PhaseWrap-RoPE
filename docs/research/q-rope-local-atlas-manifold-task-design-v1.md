# Local atlas manifold task design

## Motivation
The latent phase manifold branch failed because one bounded global-phase symbolic control and the current nonlinear manifold control family remained sufficient on the primary metric. A future task must require multiple local charts whose residual structure cannot be compressed into one global phase family or one fixed nonlinear basis.

## Preserved next angle
- future task family: `local atlas manifold residual response`

## Requirement
A future target should:
- preserve near-null coarse lookup by construction
- defeat additive analog controls by construction
- defeat the current nonlinear manifold family by construction
- defeat one bounded global-phase symbolic control by construction
- require locally charted residual routing across analog neighborhoods rather than one global residual map

## Future restart bar
Do not approve a future implementation unless the task memo proves all of the following:
- coarse lookup is near-null
- additive analog controls are insufficient
- the current nonlinear manifold family is insufficient
- the current phase-insensitive and global-phase symbolic families are insufficient
- the local-chart residual cannot be rewritten as one global transform over declared analog factors

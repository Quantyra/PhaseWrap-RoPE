# Approval-candidate memo for nonlinear manifold path

## Decision
- `ELEVATE TO APPROVAL-CANDIDATE`
- do not approve implementation in this step

## Basis
The nonlinear manifold path now has:
- one exact task
- one exact future candidate
- one fixed additive control stack
- one fixed first packet
- one explicit anti-shortcut rule

## Why this is stronger than the prior continuous branches
- coarse lookup is explicitly centered away
- additive analog controls are declared and intentionally misspecified
- the target depends on curved interaction structure rather than a sum of analog factors

## Remaining gate question
The remaining question is now implementation readiness, not design ambiguity.

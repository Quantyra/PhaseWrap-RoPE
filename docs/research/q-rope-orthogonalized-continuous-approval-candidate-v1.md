# Orthogonalized continuous approval-candidate memo

## Decision
- Elevate `synthetic_dual_orthogonalized_continuous_response` to `approval-candidate` status.

## Why it crosses the bar
- The task is exact.
- The coarse-null requirement is explicit.
- The future control stack is explicit enough for a fair bounded packet.
- The branch remains narrow: one task, one candidate, one local packet.

## Remaining open items
- exact orthogonalized target realization
- exact residual control schema
- exact within-state analog factors to expose to the future candidate and controls

## Consequence
- The next valid move is a real implementation-approval gate.
- More exploratory memo work is no longer the bottleneck.

# Approval-candidate memo for phase-sensitive manifold path

## Decision
- `ELEVATE TO APPROVAL-CANDIDATE`
- do not approve implementation in this step

## Basis
The phase-sensitive manifold path now has:
- one exact task
- one exact future candidate
- one fixed control stack that includes the previously successful nonlinear family
- one fixed first packet
- one explicit anti-shortcut rule

## Why this is stronger than the prior nonlinear task
- it preserves coarse centering
- it preserves analog-factor transparency
- it adds state-conditioned phase structure that a single global nonlinear basis should not capture if the task is implemented correctly

## Remaining gate question
The remaining question is implementation readiness and control hygiene, not design ambiguity.

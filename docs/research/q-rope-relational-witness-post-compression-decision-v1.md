# Q-RoPE Relational Witness Post-Compression Decision v1

## Decision
- `BRANCH REMAINS ACTIVE`
- `DO NOT BROADEN TO BENCHMARKS OR REMOTE`
- `NEXT BOUNDED STEP = SYMBOLIC RELATIONAL CONTROL`

## Why the branch remains active
The witness branch now has four consecutive bounded positives:
- first packet
- split-rotation hardening
- feature-group ablation
- schema compression

The strongest new result is:
- `contrasts_only` matched `full`
- `means_only` degraded

So the branch is now better specified, not just better performing.

## Why the next question changes now
The main uncertainty is no longer:
- which approved witness features matter internally

That question has been answered well enough.

The main remaining uncertainty is now:
- does the quantum-derived contrast core add anything beyond a direct symbolic relational control on the same task?

If a tiny classical head over direct symbolic sector features also solves the task perfectly, then the current branch is still useful as a design insight, but the quantum part would remain under-justified.

## Next bounded evidence step
### `symbolic relational control`
Define one control baseline that uses:
- the same tiny logistic-regression-equivalent head
- a direct symbolic relational feature schema tied to the task sectors
- no quantum-derived sector-response values

Compare it only against:
- `V_future_relational_witness` with `contrasts_only`

## Why this is the right next move
It is the tightest remaining validity question.

It tests whether the current positive result reflects:
- a genuinely useful quantum-derived relational representation
or
- a task that is linearly easy once the relational structure is made explicit in any form.

## What remains disallowed
- remote execution
- benchmark expansion
- multiple new synthetic tasks
- multiple new witness branches

## Bottom line
The witness branch has now earned one more bounded control.
That control should target the strongest remaining threat to interpretation:
- a direct symbolic relational baseline, not another internal witness tweak.

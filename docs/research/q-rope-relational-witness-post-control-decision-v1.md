# Q-RoPE Relational Witness Post-Control Decision v1

## Decision
- `BRANCH REMAINS ACTIVE`
- `CURRENT TASK IS EXHAUSTED FOR UNIQUENESS CLAIMS`
- `NEXT BOUNDED STEP = HARDER SYMBOLIC-RESISTANT TASK DESIGN`

## Why the branch remains active
The branch still achieved something real:
- it produced the strongest bounded positive line in the repository to date
- it survived split hardening
- it survived feature-group ablation
- it compressed to a clean relational contrast core

Those points should be preserved.

## Why the current task is exhausted
The direct symbolic control matched the compressed witness exactly.

That means:
- `synthetic_sector_parity_binary` is no longer useful as a differentiating test
- more runs on this task will not recover a stronger quantum-specific claim

So the branch should not be archived.
But the task should be treated as exhausted for the question that now matters.

## What the next question must be
The next valid question is:
- can the witness branch still outperform a fair symbolic control on a harder alignment-safe relational task?

That is a task-design problem now, not an internal witness-tuning problem.

## Next bounded move
### `harder symbolic-resistant task design`
Design one new synthetic task family with these properties:
- still alignment-safe
- still relational
- not linearly solved by direct sector one-hot alone
- no token-identity shortcut
- no numeric offset-sign or magnitude shortcut

## What is explicitly disallowed now
- more runs on `synthetic_sector_parity_binary`
- remote execution
- benchmark expansion
- new witness architecture branches before a new task is fixed

## Bottom line
The witness branch should continue.
The current task should not.
The next protocol move is a memo-level design of one harder alignment-safe task that can actually test whether the witness representation adds something beyond symbolic sector identity.

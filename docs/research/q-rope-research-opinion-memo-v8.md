# Q-RoPE Research Opinion Memo v8

Date: 2026-03-15

## BLUF
- `E005` did not strengthen the preserved evidence package.
- It did strengthen the boundary map by showing that bounded shared-memory repeated query reuse is not yet robust under retained token renaming.
- The correct program decision remains low-intensity continuation with no new execution by default.

## Why
- the line survived the fixed first packet but failed immediately at retained nuisance hardening `token_permutation=cdab`
- the bounded symbolic control beat the witness on both declared mean gate metrics in that hardening packet
- this is a useful negative result because it narrows the current evidence ceiling on repeated query reuse over one shared candidate memory
- it is still not enough to justify hardware, publication, or default expansion

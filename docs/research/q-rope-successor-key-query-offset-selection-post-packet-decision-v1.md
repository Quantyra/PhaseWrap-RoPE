# Q-RoPE Successor Key-Query Offset Selection Post-Packet Decision v1

## BLUF
- The witness led the bounded symbolic control on mean `mae` and mean `rank_correlation` in the fixed three-seed packet.
- The successor line remains active.
- The next valid move is one retained-model nuisance-hardening packet only.

## Decision
- `CONTINUE TO TOKEN-RENAMING HARDENING ONLY`

## Why
- The successor candidate cleared the declared first-packet gate.
- The result is materially stronger than a weak mixed-lead packet.
- The implementation stayed bounded after narrowing the feature basis to avoid numerical blow-up.

## Limits
- This is still one local simulator packet.
- It does not justify hardware reopening, external claims, or broader successor-class expansion.

# Q-RoPE Successor Key-Query Offset Selection Post-Token-Renaming Decision v1

## BLUF
- The `token_permutation=cdab` packet was non-inert.
- The witness remained ahead of the bounded symbolic control on mean `mae` and mean `rank_correlation`.
- The next valid move is the fixed structural hardening packet `pair_reindex=1` only.

## Decision
- `CONTINUE TO PAIR-REINDEX HARDENING ONLY`

## Why
- The nuisance packet did real work instead of behaving inertly.
- The witness still cleared the declared two-metric gate.
- The branch now merits structural hardening rather than broader successor-class expansion.

## Limits
- This remains one bounded local successor line.
- It does not justify hardware reopening or additional successor candidates.

# Q-RoPE Transfer Fan-In Consensus Archive Note v1

Archive status:
- `fanin-consensus` is preserved as an archived negative transfer boundary.

Why archived:
- The witness passed the base packet.
- The first nuisance hardening packet `token_permutation=cdab` reversed the branch decision.
- The bounded symbolic control then led on both declared packet metrics in the mean.

Use:
- Treat this branch as a boundary condition for future transfer-family design.
- Do not reopen execution on this family without a materially different fairness contract.

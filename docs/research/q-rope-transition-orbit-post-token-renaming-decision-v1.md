# Transition Orbit Post Token-Renaming Decision v1

## Decision
- keep the branch active
- do not count the token-renaming packet as new strength evidence

## Why
- `cdab` renaming collapses under orbit-canonical rendering
- the packet reproduced the base means instead of producing a new perturbation outcome
- the right next step is another non-inert label-preserving hardening test, not branch expansion

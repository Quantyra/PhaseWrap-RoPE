# Transition Orbit Rank-Stable Task Design v1

## Preserved next angle
- design a rank-stable transition task where relational ordering is primary and absolute response magnitude is explicitly de-emphasized by construction

## Why this is the next angle
- the stopped branch retained stronger rank behavior under the deeper hardening packet
- it failed on MAE, so the current task cannot support execution claims
- the remaining plausible value is a new task whose target is genuinely ordinal rather than absolute-response based

## Posture
- memo-only
- no implementation approval
- no reuse of the stopped execution branch without a new restart scaffold

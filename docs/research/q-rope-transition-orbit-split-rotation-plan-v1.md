# Transition Orbit Split-Rotation Hardening Plan v1

## Goal
Rerun the fixed orbit packet with `split_rotation = 1` while holding the candidate, control stack, and task definition constant.

## Why
- it perturbs example selection rather than symbolic structure
- it is smaller than a new control family
- it is the next bounded robustness question after pair reindex

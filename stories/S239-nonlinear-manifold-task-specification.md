# Story template

## Story ID and title
S239 - Nonlinear manifold task specification

## User value
As a research lead, I want the nonlinear analog manifold idea converted into one exact continuous task, so a future restart can attack the remaining additive-analog shortcut directly.

## Acceptance criteria
- Fix one exact nonlinear target rule
- Keep coarse lookup near-null by construction
- State the additive-baseline insufficiency requirement explicitly

## Outputs
- `docs/research/`
- `docs/evidence/`
- `logs/checkpoint.json`

## Dependencies
- S238

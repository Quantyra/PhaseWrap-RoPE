# Story template

## Story ID and title
S240 - Nonlinear manifold restart scaffold

## User value
As a research lead, I want the nonlinear manifold task bound to one fixed future candidate and one fixed control stack, so the next branch stays auditable and bounded.

## Acceptance criteria
- Fix one future candidate
- Fix one additive control stack
- Fix one first packet and required diagnostics

## Outputs
- `docs/research/`
- `docs/evidence/`
- `logs/checkpoint.json`

## Dependencies
- S239

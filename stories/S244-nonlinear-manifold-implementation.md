# Story template

## Story ID and title
S244 - Nonlinear manifold implementation

## User value
As a research lead, I want the approved nonlinear manifold branch implemented and run on its fixed packet, so the repo can test whether the witness line survives stronger task construction against additive symbolic controls.

## Acceptance criteria
- Implement only the approved task, candidate, and controls
- Run the fixed packet
- Emit auditable centering and regression diagnostics

## Outputs
- `src/qrope/`
- `tests/`
- `logs/ablation_runs/`
- `docs/research/`
- `docs/evidence/`
- `logs/checkpoint.json`

## Dependencies
- S243

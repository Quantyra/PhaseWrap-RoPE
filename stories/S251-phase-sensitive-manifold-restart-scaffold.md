# Story template

## Story ID and title
S251 - Phase-sensitive manifold restart scaffold

## User value
As a research lead, I want the phase-sensitive task bound to one future candidate and one fixed control stack, so the next branch stays bounded and auditable.

## Acceptance criteria
- Fix one future candidate
- Fix one control stack including the prior successful nonlinear family
- Fix one first packet and required diagnostics

## Outputs
- `docs/research/`
- `docs/evidence/`
- `logs/checkpoint.json`

## Dependencies
- S250
